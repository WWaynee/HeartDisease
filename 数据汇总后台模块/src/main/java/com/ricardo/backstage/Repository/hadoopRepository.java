package com.ricardo.backstage.Repository;

import com.ricardo.backstage.Entity.hadoop;
import org.springframework.data.jpa.repository.JpaRepository;

public interface hadoopRepository extends JpaRepository<hadoop,String> {
}
